import { COLORS, GRADIENTS } from './colors';

export const THEME = {
    colors: COLORS,
    gradients: GRADIENTS,
    spacing: {
        xs: 4,
        sm: 8,
        md: 16,
        lg: 24,
        xl: 32,
    },
    borderRadius: {
        sm: 8,
        md: 12,
        lg: 20,
        xl: 30,
        round: 100,
    },
};

export { COLORS, GRADIENTS };
